apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: elk-gw
  namespace: logging
spec:
  selector:
    istio: egressgateway
  servers:
  - hosts:
    - "elk.rezz.io"
    port:
      number: 2021
      name: tls-2021
      protocol: TLS
    tls:
      mode: PASSTHROUGH
