apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: external-host-elk-se
  namespace: logging
spec:
  exportTo:
    - "."
  location: MESH_EXTERNAL
  hosts:
  - "elk.rezz.io"
  ports:
  - name: tls-elk
    number: 9200
    protocol: HTTPS
  resolution: DNS
