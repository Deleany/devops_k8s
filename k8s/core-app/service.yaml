apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: core-app-svc
  labels:
    app: core-app
spec:
  selector:
    app: core-app
  ports:
  - name: core-app-http
    port: 8081
    targetPort: 1111
