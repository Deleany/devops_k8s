#!groovy
// Check host properties
properties([disableConcurrentBuilds()])

pipline {
  agent {
    lable 'main'
  }
  oprtions {
    buildDiscarder(logRotator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
    timestamps()
  }
  stages {
    stage ("Preparation") {
      steps {
        sh 'ssh root@192.168.88.200 \'hostname\''
      }
    }
  }
}
